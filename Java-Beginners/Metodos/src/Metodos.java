public class Metodos {
    public static void main(String[] args) throws Exception {
        // classe = atributos + metodos (funções, subrotinas)
        // critérios de nomeação: verbo no infinitivo, camelCase
        // parametros necessarios
        // metodo dentro da classe
        //retorna valor ou nao? void 
        // necessário se atentar a tratamento de erros, divisor não pode ser 0
        // visivel por todo o sistema, privado, nao visivel para outras classes, herança
    }

    public int dividir(){ 
        int a, b;
        a = 1;
        b = 4;
        return a/b; 
    }
}
